# ติดตั้งจริงที่: /etc/nginx/conf.d/dojojin-site.conf
server {
    listen 80;
    server_name dojojin.tech www.dojojin.tech localhost;

    root /var/www/dojojin-site;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
