# Cloudflare Tunnel config สำหรับรันบน host (ไม่ใช่ docker)
# ติดตั้งจริงที่: ~/.cloudflared/config-host.yml
# หมายเหตุ: ใช้ 127.0.0.1 (ไม่ใช่ host.docker.internal) และ path จริงบน host
# credentials (TunnelSecret) อยู่ในไฟล์ .json แยกต่างหาก — ไม่ commit

tunnel: f6684909-a7d7-4b0e-9d29-2328d52c1135
credentials-file: /home/kiseki/.cloudflared/f6684909-a7d7-4b0e-9d29-2328d52c1135.json

ingress:
  - hostname: dojojin.tech
    service: http://127.0.0.1:80

  - hostname: www.dojojin.tech
    service: http://127.0.0.1:80

  - hostname: ssh.dojojin.tech
    service: ssh://127.0.0.1:22

  - hostname: ai.dojojin.tech
    service: http://127.0.0.1:3000

  # docs.dojojin.tech — เพิ่มเมื่อ Phase 5 ของ DOCS-SETUP.md
  - hostname: docs.dojojin.tech
    service: http://127.0.0.1:80

  - service: http_status:404
